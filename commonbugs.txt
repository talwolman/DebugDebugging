not adding the # or "." in the qsa/qs helper functions when applicable. Misspelling variable names or forgetting to declare them. Having extra () or not enough. Using incorrect comparison operators (such as == instead of ===). Assigning information to a null reference. Mixing up syntax for various languages such as declaring ints in Javascript or trying to use the let keyword in php. forgetting to save my files before seeing if what I changed worked. forgetting to put the helper functions in my files and then trying to use them. forgetting a semi colon. forgetting a closing bracket or having one too many 
